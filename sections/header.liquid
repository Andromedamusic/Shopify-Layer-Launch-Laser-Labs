<header class="header" data-section-id="{{ section.id }}">
  <div class="header__wrapper page-width">
    {%- comment -%} Logo with Premium Styling {%- endcomment -%}
    <div class="header__logo">
      <a href="/" class="header__logo-link">
        {%- if section.settings.logo != blank -%}
          {{ section.settings.logo | image_url: width: 200 | image_tag:
            loading: 'eager',
            class: 'header__logo-image',
            alt: shop.name,
            width: 200,
            height: 55
          }}
        {%- else -%}
          <div class="header__logo-brand">
            <span class="header__logo-tagline">— Premium Custom Engraving —</span>
            <span class="header__logo-text">Layer Launch Laser Lab</span>
          </div>
        {%- endif -%}
      </a>
    </div>

    {%- comment -%} Navigation with Collections Mega-Dropdown {%- endcomment -%}
    <nav class="header__nav" role="navigation">
      <ul class="header__menu" role="list">
        {%- comment -%} Home Link {%- endcomment -%}
        <li class="header__menu-item">
          <a href="/" class="header__menu-link {% if request.path == '/' %}header__menu-link--active{% endif %}">
            Home
          </a>
        </li>

        {%- comment -%} Collections Mega-Dropdown {%- endcomment -%}
        <li class="header__menu-item header__menu-item--has-dropdown header__menu-item--mega">
          <a href="/collections" class="header__menu-link {% if request.path contains '/collections' %}header__menu-link--active{% endif %}">
            Collections
            <svg class="header__menu-chevron" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6 9 12 15 18 9"/>
            </svg>
          </a>

          <div class="header__mega-dropdown">
            <div class="header__mega-inner">
              {%- comment -%} By Material Column {%- endcomment -%}
              <div class="header__mega-column">
                <h4 class="header__mega-heading">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/></svg>
                  By Material
                </h4>
                <ul class="header__mega-list">
                  <li><a href="/collections/slate-coasters" class="header__mega-link">Slate Coasters</a></li>
                  <li><a href="/collections/wood-products" class="header__mega-link">Wood Products</a></li>
                  <li><a href="/collections/leather-goods" class="header__mega-link">Leather Goods</a></li>
                  <li><a href="/collections/acrylic" class="header__mega-link">Acrylic Products</a></li>
                  <li><a href="/collections/metal-engraving" class="header__mega-link">Metal Engraving</a></li>
                  <li><a href="/collections/glass" class="header__mega-link">Glass Etching</a></li>
                </ul>
              </div>

              {%- comment -%} By Occasion Column {%- endcomment -%}
              <div class="header__mega-column">
                <h4 class="header__mega-heading">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
                  By Occasion
                </h4>
                <ul class="header__mega-list">
                  <li><a href="/collections/wedding-gifts" class="header__mega-link">Wedding & Anniversary</a></li>
                  <li><a href="/collections/corporate-gifts" class="header__mega-link">Corporate & Business</a></li>
                  <li><a href="/collections/sports-awards" class="header__mega-link">Sports & Awards</a></li>
                  <li><a href="/collections/memorial" class="header__mega-link">Memorial & Tribute</a></li>
                  <li><a href="/collections/holiday-gifts" class="header__mega-link">Holiday Gifts</a></li>
                  <li><a href="/collections/pet-products" class="header__mega-link">Pet Products</a></li>
                </ul>
              </div>

              {%- comment -%} Popular & Featured Column {%- endcomment -%}
              <div class="header__mega-column">
                <h4 class="header__mega-heading">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
                  Featured
                </h4>
                <ul class="header__mega-list">
                  <li><a href="/collections/best-sellers" class="header__mega-link header__mega-link--featured">⭐ Best Sellers</a></li>
                  <li><a href="/collections/new-arrivals" class="header__mega-link header__mega-link--featured">🆕 New Arrivals</a></li>
                  <li><a href="/collections/sale" class="header__mega-link header__mega-link--sale">🏷️ On Sale</a></li>
                  <li><a href="/collections/golf" class="header__mega-link">Golf Ball Markers</a></li>
                  <li><a href="/collections/all" class="header__mega-link">View All Products</a></li>
                </ul>
                <div class="header__mega-cta">
                  <a href="/pages/custom-orders" class="header__mega-cta-btn">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
                    Custom Orders
                  </a>
                </div>
              </div>
            </div>
          </div>
        </li>

        {%- comment -%} Catalog Link {%- endcomment -%}
        <li class="header__menu-item">
          <a href="/collections/all" class="header__menu-link {% if request.path == '/collections/all' %}header__menu-link--active{% endif %}">
            Catalog
          </a>
        </li>

        {%- comment -%} Contact Link {%- endcomment -%}
        <li class="header__menu-item">
          <a href="/pages/contact" class="header__menu-link {% if request.path contains '/contact' %}header__menu-link--active{% endif %}">
            Contact
          </a>
        </li>
      </ul>
    </nav>

    {%- comment -%} Cart {%- endcomment -%}
    <div class="header__actions">
      <a href="/cart" class="header__cart-link">
        <svg class="header__cart-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="9" cy="21" r="1"/>
          <circle cx="20" cy="21" r="1"/>
          <path d="M1 1H5L7.68 14.39C7.77 14.8301 8.02 15.2233 8.38 15.498C8.74 15.7727 9.19 15.9113 9.64 15.89H19.36C19.81 15.9113 20.26 15.7727 20.62 15.498C20.98 15.2233 21.23 14.8301 21.32 14.39L23 6H6"/>
        </svg>
        <span class="header__cart-count">{{ cart.item_count }}</span>
      </a>
    </div>

    {%- comment -%} Mobile menu toggle {%- endcomment -%}
    <button class="header__mobile-toggle" aria-label="Menu" aria-expanded="false">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="3" y1="12" x2="21" y2="12"/>
        <line x1="3" y1="6" x2="21" y2="6"/>
        <line x1="3" y1="18" x2="21" y2="18"/>
      </svg>
    </button>
  </div>

  {%- comment -%} Mobile Menu Drawer {%- endcomment -%}
  <div class="header__mobile-overlay"></div>
  <nav class="header__mobile-drawer" aria-label="Mobile navigation">
    <div class="header__mobile-header">
      <span class="header__mobile-title">Menu</span>
      <button class="header__mobile-close" aria-label="Close menu">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="18" y1="6" x2="6" y2="18"/>
          <line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
    <ul class="header__mobile-menu" role="list">
      {%- comment -%} Home {%- endcomment -%}
      <li class="header__mobile-item">
        <a href="/" class="header__mobile-link {% if request.path == '/' %}header__mobile-link--active{% endif %}">Home</a>
      </li>

      {%- comment -%} Collections with Expandable Submenu {%- endcomment -%}
      <li class="header__mobile-item header__mobile-item--expandable">
        <div class="header__mobile-link-wrapper">
          <a href="/collections" class="header__mobile-link">Collections</a>
          <button class="header__mobile-expand" aria-label="Expand collections" aria-expanded="false">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <polyline points="6 9 12 15 18 9"/>
            </svg>
          </button>
        </div>
        <ul class="header__mobile-submenu" role="list">
          <li class="header__mobile-submenu-heading">By Material</li>
          <li><a href="/collections/slate-coasters" class="header__mobile-sublink">Slate Coasters</a></li>
          <li><a href="/collections/wood-products" class="header__mobile-sublink">Wood Products</a></li>
          <li><a href="/collections/leather-goods" class="header__mobile-sublink">Leather Goods</a></li>
          <li><a href="/collections/acrylic" class="header__mobile-sublink">Acrylic Products</a></li>
          <li><a href="/collections/metal-engraving" class="header__mobile-sublink">Metal Engraving</a></li>
          <li><a href="/collections/glass" class="header__mobile-sublink">Glass Etching</a></li>

          <li class="header__mobile-submenu-heading">By Occasion</li>
          <li><a href="/collections/wedding-gifts" class="header__mobile-sublink">Wedding & Anniversary</a></li>
          <li><a href="/collections/corporate-gifts" class="header__mobile-sublink">Corporate & Business</a></li>
          <li><a href="/collections/sports-awards" class="header__mobile-sublink">Sports & Awards</a></li>
          <li><a href="/collections/memorial" class="header__mobile-sublink">Memorial & Tribute</a></li>
          <li><a href="/collections/pet-products" class="header__mobile-sublink">Pet Products</a></li>

          <li class="header__mobile-submenu-heading">Featured</li>
          <li><a href="/collections/best-sellers" class="header__mobile-sublink">⭐ Best Sellers</a></li>
          <li><a href="/collections/new-arrivals" class="header__mobile-sublink">🆕 New Arrivals</a></li>
          <li><a href="/collections/sale" class="header__mobile-sublink header__mobile-sublink--sale">🏷️ On Sale</a></li>
          <li><a href="/collections/all" class="header__mobile-sublink">View All Products</a></li>
        </ul>
      </li>

      {%- comment -%} Catalog {%- endcomment -%}
      <li class="header__mobile-item">
        <a href="/collections/all" class="header__mobile-link">Catalog</a>
      </li>

      {%- comment -%} Contact {%- endcomment -%}
      <li class="header__mobile-item">
        <a href="/pages/contact" class="header__mobile-link">Contact</a>
      </li>

      {%- comment -%} Custom Orders CTA {%- endcomment -%}
      <li class="header__mobile-item header__mobile-item--cta">
        <a href="/pages/custom-orders" class="header__mobile-link header__mobile-link--cta">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
          Custom Orders
        </a>
      </li>
    </ul>
    <div class="header__mobile-footer">
      <a href="/cart" class="header__mobile-cart">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="9" cy="21" r="1"/>
          <circle cx="20" cy="21" r="1"/>
          <path d="M1 1H5L7.68 14.39C7.77 14.8301 8.02 15.2233 8.38 15.498C8.74 15.7727 9.19 15.9113 9.64 15.89H19.36C19.81 15.9113 20.26 15.7727 20.62 15.498C20.98 15.2233 21.23 14.8301 21.32 14.39L23 6H6"/>
        </svg>
        Cart ({{ cart.item_count }})
      </a>
    </div>
  </nav>
</header>

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(255, 255, 255, 0.98);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(0, 0, 0, 0.08);
    box-shadow: 0 2px 12px rgba(0,0,0,0.04);
  }

  @media (prefers-color-scheme: dark) {
    .header {
      background: rgba(13, 13, 13, 0.95);
      border-bottom-color: rgba(255, 255, 255, 0.1);
    }
  }

  .header__wrapper {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 24px;
    gap: 2rem;
  }

  .header__logo-link {
    display: flex;
    align-items: center;
    text-decoration: none;
  }

  .header__logo-image {
    max-height: 55px;
    width: auto;
  }

  /* Premium Branding Styles */
  .header__logo-brand {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }

  .header__logo-tagline {
    font-size: 1rem;
    font-weight: 500;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #c9a227;
    margin-bottom: 2px;
  }

  .header__logo-text {
    font-size: 2rem;
    font-weight: 800;
    color: #1a1a1a;
    letter-spacing: -0.02em;
    line-height: 1.1;
    text-transform: uppercase;
  }

  @media (prefers-color-scheme: dark) {
    .header__logo-text {
      color: #ffffff;
    }
  }

  .header__nav {
    flex: 1;
    display: flex;
    justify-content: center;
  }

  /* Expanded Navigation */
  .header__menu {
    display: flex;
    gap: 0.5rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .header__menu-item {
    position: relative;
  }

  .header__menu-link {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 12px 24px;
    font-size: 1.5rem;
    font-weight: 600;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    color: #1a1a1a;
    text-decoration: none;
    border-radius: 6px;
    transition: all 0.25s ease;
  }

  @media (prefers-color-scheme: dark) {
    .header__menu-link {
      color: #ffffff;
    }
  }

  .header__menu-link:hover {
    color: #c9a227;
    background: rgba(201, 162, 39, 0.08);
    text-decoration: none;
  }

  .header__menu-link--active {
    color: #c9a227;
    background: rgba(201, 162, 39, 0.1);
    position: relative;
  }

  .header__menu-link--active::after {
    content: '';
    position: absolute;
    bottom: 8px;
    left: 24px;
    right: 24px;
    height: 2px;
    background: #c9a227;
    border-radius: 1px;
  }

  .header__menu-chevron {
    transition: transform 0.2s;
    width: 14px;
    height: 14px;
  }

  .header__menu-item:hover .header__menu-chevron {
    transform: rotate(180deg);
  }

  .header__dropdown {
    position: absolute;
    top: 100%;
    left: -12px;
    min-width: 220px;
    background: white;
    border: 1px solid #eee;
    border-radius: 8px;
    box-shadow: 0 8px 24px rgba(0,0,0,0.12);
    padding: 10px 0;
    list-style: none;
    opacity: 0;
    visibility: hidden;
    transform: translateY(10px);
    transition: all 0.2s ease;
  }

  .header__menu-item:hover .header__dropdown {
    opacity: 1;
    visibility: visible;
    transform: translateY(4px);
  }

  .header__dropdown-link {
    display: block;
    padding: 10px 20px;
    font-size: 1.5rem;
    color: #1a1a1a;
    text-decoration: none;
    transition: background 0.2s, color 0.2s;
  }

  .header__dropdown-link:hover {
    background: #f8f8f8;
    color: #c9a227;
    text-decoration: none;
  }

  /* ============================================
     Mega Dropdown Styles
     ============================================ */
  .header__mega-dropdown {
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%) translateY(10px);
    min-width: 700px;
    background: white;
    border: 1px solid rgba(0, 0, 0, 0.08);
    border-radius: 12px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
    padding: 24px;
    opacity: 0;
    visibility: hidden;
    transition: all 0.25s ease;
    z-index: 1000;
  }

  .header__menu-item--mega:hover .header__mega-dropdown {
    opacity: 1;
    visibility: visible;
    transform: translateX(-50%) translateY(4px);
  }

  .header__mega-inner {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }

  .header__mega-column {
    min-width: 180px;
  }

  .header__mega-heading {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 1.3rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: #1a1a1a;
    margin: 0 0 12px;
    padding-bottom: 10px;
    border-bottom: 2px solid #c9a227;
  }

  .header__mega-heading svg {
    color: #c9a227;
  }

  .header__mega-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .header__mega-list li {
    margin-bottom: 2px;
  }

  .header__mega-link {
    display: block;
    padding: 8px 12px;
    font-size: 1.4rem;
    color: #444;
    text-decoration: none;
    border-radius: 6px;
    transition: all 0.2s;
  }

  .header__mega-link:hover {
    background: rgba(201, 162, 39, 0.1);
    color: #c9a227;
    text-decoration: none;
    padding-left: 16px;
  }

  .header__mega-link--featured {
    font-weight: 600;
    color: #1a1a1a;
  }

  .header__mega-link--sale {
    color: #c62828;
    font-weight: 600;
  }

  .header__mega-cta {
    margin-top: 16px;
    padding-top: 16px;
    border-top: 1px solid #eee;
  }

  .header__mega-cta-btn {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 16px;
    background: linear-gradient(135deg, #1a1a1a 0%, #333 100%);
    color: white;
    font-size: 1.4rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 8px;
    transition: all 0.2s;
  }

  .header__mega-cta-btn:hover {
    background: linear-gradient(135deg, #c9a227 0%, #d4b340 100%);
    color: white;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(201, 162, 39, 0.3);
  }

  .header__mega-cta-btn svg {
    flex-shrink: 0;
  }

  /* Dark mode for mega dropdown */
  @media (prefers-color-scheme: dark) {
    .header__mega-dropdown {
      background: #1a1a1a;
      border-color: rgba(255, 255, 255, 0.1);
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
    }

    .header__mega-heading {
      color: #ffffff;
    }

    .header__mega-link {
      color: #e0e0e0;
    }

    .header__mega-link:hover {
      background: rgba(201, 162, 39, 0.15);
      color: #d4b340;
    }

    .header__mega-link--featured {
      color: #ffffff;
    }

    .header__mega-link--sale {
      color: #ff6b6b;
    }

    .header__mega-cta {
      border-top-color: rgba(255, 255, 255, 0.1);
    }
  }

  .header__actions {
    display: flex;
    align-items: center;
    gap: 1.25rem;
  }

  .header__cart-link {
    position: relative;
    display: flex;
    align-items: center;
    color: #1a1a1a;
    padding: 8px;
    transition: color 0.2s;
  }
  .header__cart-link:hover {
    color: #c9a227;
  }
  .header__cart-icon {
    width: 26px;
    height: 26px;
  }

  .header__cart-count {
    position: absolute;
    top: 0;
    right: 0;
    min-width: 20px;
    height: 20px;
    padding: 0 5px;
    background: #c9a227;
    color: white;
    font-size: 1.2rem;
    font-weight: 600;
    line-height: 20px;
    text-align: center;
    border-radius: 10px;
  }

  .header__mobile-toggle {
    display: none;
    background: none;
    border: none;
    padding: 10px;
    cursor: pointer;
    color: #1a1a1a;
  }
  .header__mobile-toggle svg {
    width: 26px;
    height: 26px;
  }

  @media (max-width: 989px) {
    .header__wrapper {
      padding: 14px 20px;
    }
    .header__logo-text {
      font-size: 1.9rem;
    }
    .header__nav {
      display: none;
    }
    .header__mobile-toggle {
      display: block;
    }
  }

  /* Mobile Menu Drawer Styles */
  .header__mobile-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
    z-index: 999;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s, visibility 0.3s;
  }

  .header__mobile-overlay.is-visible {
    opacity: 1;
    visibility: visible;
  }

  .header__mobile-drawer {
    position: fixed;
    top: 0;
    right: 0;
    width: 85%;
    max-width: 320px;
    height: 100%;
    background: white;
    z-index: 1000;
    transform: translateX(100%);
    transition: transform 0.3s ease;
    display: flex;
    flex-direction: column;
    box-shadow: -4px 0 20px rgba(0, 0, 0, 0.15);
  }

  .header__mobile-drawer.is-open {
    transform: translateX(0);
  }

  .header__mobile-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px;
    border-bottom: 1px solid #eee;
  }

  .header__mobile-title {
    font-size: 1.8rem;
    font-weight: 700;
    color: #1a1a1a;
  }

  .header__mobile-close {
    background: none;
    border: none;
    padding: 8px;
    cursor: pointer;
    color: #1a1a1a;
  }

  .header__mobile-menu {
    list-style: none;
    padding: 0;
    margin: 0;
    flex: 1;
    overflow-y: auto;
  }

  .header__mobile-item {
    border-bottom: 1px solid #f0f0f0;
  }

  .header__mobile-link {
    display: block;
    padding: 18px 20px;
    font-size: 1.7rem;
    font-weight: 500;
    color: #1a1a1a;
    text-decoration: none;
    transition: background 0.2s, color 0.2s;
  }

  .header__mobile-link:hover,
  .header__mobile-link--active {
    background: #f8f8f8;
    color: #c9a227;
  }

  .header__mobile-submenu {
    list-style: none;
    padding: 0;
    margin: 0;
    background: #f8f8f8;
  }

  .header__mobile-sublink {
    display: block;
    padding: 14px 20px 14px 36px;
    font-size: 1.5rem;
    color: #666;
    text-decoration: none;
  }

  .header__mobile-sublink:hover {
    color: #c9a227;
  }

  .header__mobile-sublink--sale {
    color: #c62828;
    font-weight: 600;
  }

  /* Mobile Expandable Menu Styles */
  .header__mobile-link-wrapper {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .header__mobile-link-wrapper .header__mobile-link {
    flex: 1;
  }

  .header__mobile-expand {
    background: none;
    border: none;
    padding: 18px 20px;
    cursor: pointer;
    color: #1a1a1a;
    transition: transform 0.3s;
  }

  .header__mobile-expand[aria-expanded="true"] {
    transform: rotate(180deg);
  }

  .header__mobile-item--expandable .header__mobile-submenu {
    display: none;
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
  }

  .header__mobile-item--expandable.is-expanded .header__mobile-submenu {
    display: block;
    max-height: 1000px;
  }

  .header__mobile-submenu-heading {
    padding: 12px 20px 8px 36px;
    font-size: 1.2rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: #c9a227;
    background: transparent;
  }

  .header__mobile-item--cta {
    margin-top: 10px;
    padding: 0 20px;
  }

  .header__mobile-link--cta {
    display: flex !important;
    align-items: center;
    gap: 10px;
    background: linear-gradient(135deg, #c9a227 0%, #d4b340 100%) !important;
    color: white !important;
    border-radius: 8px;
    justify-content: center;
  }

  .header__mobile-link--cta:hover {
    background: linear-gradient(135deg, #b8922a 0%, #c9a227 100%) !important;
  }

  /* Dark mode mobile styles */
  @media (prefers-color-scheme: dark) {
    .header__mobile-drawer {
      background: #0d0d0d;
    }

    .header__mobile-header {
      border-bottom-color: rgba(255, 255, 255, 0.1);
    }

    .header__mobile-title {
      color: #ffffff;
    }

    .header__mobile-close {
      color: #ffffff;
    }

    .header__mobile-item {
      border-bottom-color: rgba(255, 255, 255, 0.05);
    }

    .header__mobile-link {
      color: #ffffff;
    }

    .header__mobile-link:hover,
    .header__mobile-link--active {
      background: rgba(255, 255, 255, 0.05);
      color: #d4b340;
    }

    .header__mobile-submenu {
      background: rgba(255, 255, 255, 0.03);
    }

    .header__mobile-sublink {
      color: #d0d0d0;
    }

    .header__mobile-sublink:hover {
      color: #d4b340;
    }

    .header__mobile-expand {
      color: #ffffff;
    }

    .header__mobile-footer {
      border-top-color: rgba(255, 255, 255, 0.1);
    }

    .header__mobile-cart {
      background: rgba(255, 255, 255, 0.1);
    }

    .header__mobile-cart:hover {
      background: rgba(255, 255, 255, 0.15);
    }
  }

  .header__mobile-footer {
    padding: 20px;
    border-top: 1px solid #eee;
  }

  .header__mobile-cart {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 14px 20px;
    background: #1a1a1a;
    color: white;
    text-decoration: none;
    border-radius: 8px;
    font-size: 1.6rem;
    font-weight: 500;
    justify-content: center;
    transition: background 0.2s;
  }

  .header__mobile-cart:hover {
    background: #333;
    color: white;
  }
</style>

<script>
  // Mobile menu toggle - wrapped in DOMContentLoaded for safety
  document.addEventListener('DOMContentLoaded', function() {
    const mobileToggle = document.querySelector('.header__mobile-toggle');
    const mobileDrawer = document.querySelector('.header__mobile-drawer');
    const mobileOverlay = document.querySelector('.header__mobile-overlay');
    const mobileClose = document.querySelector('.header__mobile-close');

    function openMobileMenu() {
      if (!mobileDrawer || !mobileOverlay || !mobileToggle) return;
      mobileDrawer.classList.add('is-open');
      mobileOverlay.classList.add('is-visible');
      mobileToggle.setAttribute('aria-expanded', 'true');
      document.body.style.overflow = 'hidden';
    }

    function closeMobileMenu() {
      if (!mobileDrawer || !mobileOverlay || !mobileToggle) return;
      mobileDrawer.classList.remove('is-open');
      mobileOverlay.classList.remove('is-visible');
      mobileToggle.setAttribute('aria-expanded', 'false');
      document.body.style.overflow = '';
    }

    if (mobileToggle) {
      mobileToggle.addEventListener('click', openMobileMenu);
    }

    if (mobileClose) {
      mobileClose.addEventListener('click', closeMobileMenu);
    }

    if (mobileOverlay) {
      mobileOverlay.addEventListener('click', closeMobileMenu);
    }

    // Mobile expandable submenu
    document.querySelectorAll('.header__mobile-expand').forEach(button => {
      button.addEventListener('click', function() {
        const parent = this.closest('.header__mobile-item--expandable');
        if (!parent) return;
        const isExpanded = parent.classList.contains('is-expanded');

        // Close all other expanded items
        document.querySelectorAll('.header__mobile-item--expandable.is-expanded').forEach(item => {
          if (item !== parent) {
            item.classList.remove('is-expanded');
            const expandBtn = item.querySelector('.header__mobile-expand');
            if (expandBtn) expandBtn.setAttribute('aria-expanded', 'false');
          }
        });

        // Toggle current item
        parent.classList.toggle('is-expanded');
        this.setAttribute('aria-expanded', !isExpanded);
      });
    });

    // Close mobile menu on escape key
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape' && mobileDrawer && mobileDrawer.classList.contains('is-open')) {
        closeMobileMenu();
      }
    });
  });
</script>

{% schema %}
{
  "name": "Header",
  "tag": "section",
  "class": "section-header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo image"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu",
      "default": "main-menu"
    }
  ]
}
{% endschema %}
